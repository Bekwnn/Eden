// Holds state about the entity's visual representation in the scene
pub const SceneComp = struct {
    //TODO mesh etc
    shader: u32 = 0, //TODO how should the separation of presentation and game logic work?
};
